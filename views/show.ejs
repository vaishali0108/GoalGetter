<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main  bg-zinc-900 w-full min-h-screen p-10">
        <a class="text-blue-500 ml-10" href="/">Go to Home</a>
        <div class="tasks p-10 text-white ">
            <%
            let tempFile = filename.replace('.txt', ''); 
            tempFile = tempFile[0].toUpperCase() + tempFile.slice(1);
              %>
            <div class="task min-w-72 max-  px-3 py-4 bg-zinc-600 rounded-md">
                <h1 class="text-3xl text-yellow-500"><%=tempFile%></h1>
                </div>
                    <div class="task mt-5 min-w-72  px-3 py-4 bg-zinc-600 rounded-md">
                      <p><%=filedata%>
                      </p>
                    </div>
        </div>
    </div>
</body>
</html>