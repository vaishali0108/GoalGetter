<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main  bg-zinc-900 w-full min-h-screen">
        <div class="form p-10 text-white ">
            <form action="/create" method="Post">
                <input class="block w-full px-5 py-2 rounded-md bg-zinc-700 outline-none" type="text" placeholder="Title goes here" name="title">
                <textarea class="block w-full px-5 py-2 mt-2 bg-zinc-700 rounded-md resize-none outline-none" id="" name="details" placeholder="Write you task description"></textarea>
                <input class="px-5 py-2 bg-blue-600 mt-2 rounded-md cursor-pointer" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 text-white flex gap-3 flea-wrap">
            
            <% if (files.length > 0) { %> 
                <% files.forEach(function(val) { %>
                   
                    <div class="task min-w-72  px-3 py-4 bg-zinc-600 rounded-md">
                        <%
                       let tempFile = val.replace('.txt', ''); 
                       
                       tempFile = tempFile[0].toUpperCase() + tempFile.slice(1);
                      
     
                         %>
                        

                       
                        <h1 class="text-3xl tracking-tighter"><%=tempFile%></h1>
                        
                        <a class="text-blue-500 inline-block mt-3" href="/files/<%=val%>">Read More..</a>
                        <a class="text-zinc-300 inline-block mt-3" href="/files/<%=val%>/edit">Edit Filename</a>
                    </div>
                <% }) %>
            <% } else{%>
            <h1 class="text-zinc-600">No Tasks Yet.</h1>
           <%}%>
        </div>
    </div>
</body>
</html>